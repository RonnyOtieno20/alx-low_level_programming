#include "main.h"
/**
* mul - multiplies two integers.
* @a: first integer
* @b: second integer
*
* Return: the multiplication result.
*/
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
